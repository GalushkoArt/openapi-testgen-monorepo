swagger: "2.0"
info:
  title: Swagger Minimal API
  version: "1.0"
host: api.example.test
basePath: /v1
schemes:
  - https
produces:
  - application/json
x-root-note: parser-fixture
paths:
  /pets:
    get:
      operationId: listPets
      x-operation-note: parser-fixture
      parameters:
        - name: limit
          in: query
          required: true
          type: integer
          format: int32
          minimum: 1
          maximum: 100
          default: 10
      responses:
        "200":
          description: OK
          schema:
            type: object
            required:
              - items
            properties:
              items:
                type: array
                items:
                  type: string
          examples:
            application/json:
              items:
                - cat
